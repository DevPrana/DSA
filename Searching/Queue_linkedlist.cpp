#include <iostream>
struct node{
    int information;
    int rear,front;
    struct node* next;
};
void enqueue(int item){
    node *p;
    p=(struct node*) malloc(sizeof(struct node));
    p->information;
    p->next=NULL;
    if(rear==NULL||front==NULL){
        
    }
}